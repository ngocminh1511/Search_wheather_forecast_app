# ── Web framework ─────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# ── GRIB2 / meteorological data ───────────────────────────────────────────────
cfgrib>=0.9.12
xarray>=2024.2.0
eccodes>=1.7.0          # system lib wrapper; install eccodes via brew/apt first
numpy>=1.26.0

# ── Tile generation ───────────────────────────────────────────────────────────
Pillow>=10.3.0          # PNG encode + bicubic resize
mercantile>=1.2.1       # Web Mercator tile math (lat/lon ↔ z/x/y)
matplotlib>=3.8.0       # colormaps (turbo, Blues, YlGnBu, etc.)
rasterio>=1.3.0         # GDAL warp: equirectangular → EPSG:3857 reprojection
scipy>=1.10.0           # Morphological ops (binary_opening) for precip classifier

# ── Scheduler ─────────────────────────────────────────────────────────────────
APScheduler>=3.10.4

# ── HTTP (download from NOAA) ─────────────────────────────────────────────────
# uses stdlib urllib only — no extra dep needed

# ── Bunny.net Storage CDN sync (httpx for parallel uploads) ──────────────────
httpx>=0.27.0           # Bunny Storage API client

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.1    # load .env
psutil>=5.9.0           # hardware metrics monitoring
zstandard>=0.21.0       # Zstd compression for wind_field WFLD tiles

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=7.4.0           # testing framework
hypothesis>=6.92.0      # property-based testing
